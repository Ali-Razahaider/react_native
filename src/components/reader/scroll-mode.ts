export type ScrollMode = 'single' | 'continuous';

export const ScrollModes: ScrollMode[] = ['single', 'continuous'];

export const ScrollModeLabels: Record<ScrollMode, string> = {
  single: 'Single page',
  continuous: 'Continuous scroll',
};